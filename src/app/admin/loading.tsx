import { SegmentLoading } from "@/components/ui/SegmentLoading";

export default function Loading() {
  return (
    <SegmentLoading
      title="Cargando modulo administrativo"
      subtitle="Estamos preparando agenda, disenos y operaciones para que navegues sin bloqueos."
    />
  );
}
