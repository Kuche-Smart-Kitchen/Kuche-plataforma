import { SegmentLoading } from "@/components/ui/SegmentLoading";

export default function Loading() {
  return (
    <SegmentLoading
      title="Cargando dashboard"
      subtitle="Sincronizando cotizador, levantamiento y seguimiento para continuar rapido."
    />
  );
}
